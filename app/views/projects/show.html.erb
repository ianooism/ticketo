<h1>Project</h1>

<%= render project %>
<div>
  <%= render 'footer' %>
  <% if project.owner?(current_user) %>
    | <%= render 'actions_owner', project: project %>
  <% end %>
</div>

<%= render 'tickets/index', project: project, tickets: tickets %>

<h1>Create Ticket</h1>

<%= render 'tickets/form', project: project, ticket: ticket %>
