<h1>Tickets</h1>

<div><%= link_to 'Create Ticket', new_project_ticket_path(project) %></div>

<% tickets.each do |ticket| %>
  <%= render ticket %>
  <%= render 'tickets/action', project: project, ticket: ticket %>
<% end %>
